# Demo Audio Assets Directory

Este diretório armazena faixas de áudio demo locais (`.mp3`, `.wav`, `.ogg`).
O reprodutor consome faixas configuradas em `js/mockData.js` e suporta URLs locais dinâmicas via `URL.createObjectURL(file)`.
